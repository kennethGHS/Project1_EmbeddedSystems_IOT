const server = "http://localhost:8000"

const lights_url = server + "/api/lights";
const sensors_url = server + "/api/sensors";
const model_url = server + "/common/model.html";
const auth_url = server + "/api/auth";
const login_url = server + "/common/login.html";
const session_url = server + "/api/session";
const sign_url = server + "/api/sign";
const take_picture_url = server + "/api/take_picture";
const get_picture_url = server + "/api/picture";